# Frontend Environment Variables (all optional for local LAN testing)
#
# When unset, the app auto-uses http://<current-hostname>:8000
# so mobile devices on the same Wi-Fi reach your laptop backend.
#
# Override only when needed:
# VITE_API_URL=https://your-backend.onrender.com
